import { Meta, StoryObj } from '@storybook/react';

import TopRating from '.';

const meta: Meta<typeof TopRating> = {
    title: 'ui/Top Rating',
    component: TopRating,
};

type Story = StoryObj<typeof TopRating>;

export const Index: Story = {};

export default meta;
